---
roles:
  - name: bsmeding.docker
collections:
  # Pin to avoid Galaxy HTTP 500 when fetching metadata for 5.0.5
  - name: community.docker
    version: "5.1.0"
  - name: community.general
  - name: ansible.utils
